# -*- ruby -*-

require 'rubygems'
require './lib/phonenormalize.rb'

begin
  require 'jeweler'
  Jeweler::Tasks.new do |gemspec|
    gemspec.name = "phonenormalize"
    gemspec.summary = "Phone normalization class"
    gemspec.description = "Gem to parse and normalize phone numbers"
    gemspec.email = "kognate@gmail.com"
    gemspec.homepage = "http://github.com/kognate/phonenormalize"
    gemspec.description = "TODO"
    gemspec.authors = ["Josh Smith"]
  end
rescue LoadError
  puts "Jeweler not available. Install it with: sudo gem install technicalpickles-jeweler -s http://gems.github.com"
end

# vim: syntax=Ruby
